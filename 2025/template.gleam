import gleam/io

// Note: To read files in Gleam, typically you'd use a package like `simplifile`
// or target JavaScript and use FFI. 
// For this template, we'll just print a message.
pub fn main() {
  io.println("Hello from Gleam! Setup your gleam.toml dependency (e.g. simplifile) to read files.")
}
